export const metadata = {
  title: 'Thank You',
  // Conversion/confirmation page — keep out of the search index.
  robots: {
    index: false,
    follow: false,
  },
};

export default function ThankYouLayout({ children }) {
  return children;
}
